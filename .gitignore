# Android Studio / Gradle
.gradle/
.idea/
local.properties
*.iml
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# vcpkg
vcpkg_installed/
vcpkg/buildtrees/
vcpkg/packages/
vcpkg/downloads/

# CMake
CMakeFiles/
CMakeCache.txt
cmake-build-*/

# Pre-built native artifacts (reproducible via docs/ENVIRONMENT.md, not committed)
prebuilt/

# Local smoke-test APK builds for informal sharing - never published, never tracked
/smoke/

# OS
.DS_Store
Thumbs.db

# Claude's private working memory (not for the tracked repo history)
CLAUDEMEMORY.md

# Internal workflow notes - kept locally, not shown to public repo visitors
CONTRIBUTING.md

# Full source is already public - up to consumers to diff against upstream
# hypseus-singe themselves if they want to know what changed, not something
# this repo needs to hand-hold with a public changelog-style doc
docs/ANDROID_PATCHES.md
