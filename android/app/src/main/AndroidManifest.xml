<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">

    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- External mouse input events -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <!-- Audio recording support -->
    <!-- if you want to record audio, uncomment this. -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->
    <!-- <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" /> -->

    <!-- Camera support -->
    <!-- if you want to record video, uncomment this. -->
    <!--
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    -->

    <!-- Allow downloading to the external storage on Android 5.1 and older -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="22" /> -->

    <!-- Allow access to Bluetooth devices -->
    <!-- Currently this is just for Steam Controller support and requires setting SDL_HINT_JOYSTICK_HIDAPI_STEAM -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" /> -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> -->

    <!-- Allow access to the vibrator -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Deliberately NOT requesting MANAGE_EXTERNAL_STORAGE (All Files
         Access) - that grants read/write/delete over the entire device,
         not just the folders the user picks. Using Storage Access
         Framework instead (see MainActivity.kt): scoped to exactly the
         folder(s) the user explicitly selects, nothing else. -->

    <!-- #76 - Android 13+ (API 33+) blocks raw-path reads of image files
         (cover art) even inside a folder the user picked via SAF, on at
         least some devices/OEMs - confirmed via a real MediaProvider
         "Permission ... is denied" error on a real Android 13 tablet.
         READ_MEDIA_IMAGES only exists as a concept on API 33+ and is a
         no-op on everything below that (including the Retroid Pocket 5),
         so this can't change behavior there. Much narrower than All
         Files Access above - covers only image files, not the whole
         device. Requested at runtime, gated to SDK_INT >= 33, in
         MainActivity.kt. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Allow access to Internet -->
    <!-- if you want to connect to the network or internet, uncomment this. -->
    <!--
    <uses-permission android:name="android.permission.INTERNET" />
    -->

    <!-- Create a Java class extending SDLActivity and place it in a
         directory under app/src/main/java matching the package, e.g. app/src/main/java/com/gamemaker/game/MyGame.java

         then replace "SDLActivity" with the name of your class (e.g. "MyGame")
         in the XML below.

         An example Java class can be found in README-android.md
    -->
    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:enableOnBackInvokedCallback="false" >

        <!-- Example of setting SDL hints from AndroidManifest.xml:
        <meta-data android:name="SDL_ENV.SDL_ANDROID_TRAP_BACK_BUTTON" android:value="0"/>
         -->

        <!-- The real app entry point: Kotlin/Compose dashboard (folder
             picker, game list, settings). HypseusActivity (below) is
             launched explicitly from here with constructed CLI-equivalent
             args once the user picks a game - it's not the launcher. -->
        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/LauncherTheme"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- #85 - runs in its own private process, separate from the
             dashboard's. SDL deliberately allows native main() to run only
             once per process, and calls System.exit(0) if a second run is
             attempted - which previously killed the entire app, dashboard
             included, on every second game launch. Isolated here, that
             teardown only ends the game's own process (see
             HypseusActivity.onDestroy()), leaving the dashboard process
             and all of its in-memory state untouched, and giving each game
             session the fresh SDL/hypseus globals both actually require.
             launchMode deliberately left as SDL's own template default -
             changing process and task behavior together would make any
             regression harder to isolate. -->
        <activity android:name="HypseusActivity"
            android:process=":hypseus"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|grammaticalGender|fontScale|fontWeightAdjustment|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true"
            android:hardwareAccelerated="false"
            >
            <!-- Let Android know that we can handle some USB devices and should receive this event -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <!-- Drop file event -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>
