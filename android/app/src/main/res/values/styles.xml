<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme, used by HypseusActivity (the game view) -
         fullscreen/no title bar makes sense there. -->
    <style name="AppTheme" parent="android:Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
    </style>

    <!-- MainActivity (dashboard/settings/folder browser) is a normal app
         screen, not fullscreen immersive like the game itself. -->
    <style name="LauncherTheme" parent="android:Theme.Material.Light.NoActionBar">
    </style>
</resources>
